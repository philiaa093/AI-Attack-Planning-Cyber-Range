# Threat Model

Status: `IMPLEMENTED`; runtime: `UNRESOLVED`.

Assets: lab isolation, scenario integrity, planner policy, evidence integrity, and research validity.

Threats: prompt injection, malformed state, scope confusion, policy bypass, evidence tampering, replay corruption, and data leakage.

Controls: typed schemas, strict allowlists, lab-mode assertion, bounded capabilities, approval gates, refusal tests, redaction, hashes, deterministic run IDs, and restore procedures.

Control implementation and effectiveness remain `UNRESOLVED`; this scaffold closes no threat experimentally.
