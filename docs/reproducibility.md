# Reproducibility

Status: `IMPLEMENTED`; runtime: `UNRESOLVED`.

Replay package needs baseline identifier, environment versions, manifest hash, catalog hash, planner config, seed, input/output hashes, UTC timestamps, and exact commands. Redact sensitive data. Replay proves same-artifact behavior only, not generalization or production readiness.
